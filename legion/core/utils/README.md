# Core Utils

This package houses utility helpers shared across Legion.

* `sync_chroma_client.py` – synchronous ChromaDB client
* `async_chroma_client.py` – asynchronous ChromaDB client

Use the sync client when calling from synchronous code paths and the async client when inside `async` functions.
