# TherapistAgent

**Role:** No description

## Methods

- `start_heartbeat()` - No summary
- `heartbeat()` - No summary
- `set_log_paths(log_path)` - No summary
- `read_logs()` - No summary
- `compose_summary()` - No summary
- `validate_request(content, context)` - Validate incoming request against core directives:
- `fallback_response(reason)` - Generate a safe fallback message for invalid or out-of-scope requests.
- `handle_self_assessment(content, context)` - No summary
- `retrieve_memories(embeddings)` - No summary
