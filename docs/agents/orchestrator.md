# Orchestrator

**Role:** Simple orchestrator routing requests to agents.

## Methods

- `dispatch(cls, request)` - Validate via therapist and route to the requested agent.
- `get_agent_by_key(cls, key)` - No summary
- `send_response(response, channel)` - No summary
